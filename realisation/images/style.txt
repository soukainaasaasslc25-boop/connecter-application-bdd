<?php 
require_once 'functions.php'; 

$mot_cle = $_GET['q'] ?? '';

if (!empty($mot_cle)) {
    $liste_recettes = searchRecipes($pdo, $mot_cle);
} elseif (!empty($_GET['cat'])) {
    $liste_recettes = filterByCategory($pdo, $_GET['cat']);
} elseif (!empty($_GET['sort'])) {
    $liste_recettes = sortRecipes($pdo, $_GET['sort']);
} else {
    $liste_recettes = obtenirRecettes($pdo, $mot_cle);
}
?>

<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>GoldenFork • Recettes d'Exception</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;700&family=Inter:wght@400;500;600&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Inter', system-ui, sans-serif;
        }
        .heading-font {
            font-family: 'Playfair Display', serif;
        }
        .card {
            transition: all 0.3s ease;
        }
        .card:hover {
            transform: translateY(-8px);
            box-shadow: 0 20px 25px -5px rgb(0 0 0 / 0.1), 0 8px 10px -6px rgb(0 0 0 / 0.1);
        }
        .gold-accent {
            color: #D4AF37;
        }
    </style>
</head>
<body class="bg-[#F9F5F0] text-gray-800">

    <!-- Header -->
    <header class="border-b border-gray-200 bg-white sticky top-0 z-50">
        <div class="max-w-7xl mx-auto px-6 py-5 flex items-center justify-between">
            <div class="flex items-center gap-3">
                <span class="text-3xl">🍽️</span>
                <div>
                    <h1 class="heading-font text-2xl font-semibold tracking-tight">GoldenFork</h1>
                    <p class="text-xs text-gray-500 -mt-1">RECETTES D'EXCEPTION</p>
                </div>
            </div>
            
            <nav class="flex items-center gap-8 text-sm">
                <a href="#" class="hover:text-[#D4AF37] transition">Accueil</a>
                <a href="#" class="hover:text-[#D4AF37] transition">Recettes</a>
                <a href="#" class="hover:text-[#D4AF37] transition">Catégories</a>
                <a href="#" class="hover:text-[#D4AF37] transition">À propos</a>
            </nav>
        </div>
    </header>

    <!-- Hero Section (style luxe comme ton image) -->
    <div class="bg-[#F9F5F0] py-16">
        <div class="max-w-7xl mx-auto px-6 text-center">
            <h2 class="heading-font text-5xl font-light tracking-tight mb-4">
                Découvrez nos <span class="gold-accent">recettes raffinées</span>
            </h2>
            <p class="text-gray-600 max-w-md mx-auto">
                Des créations culinaires élégantes pour des moments inoubliables
            </p>
        </div>
    </div>

    <!-- Filtres & Recherche -->
    <div class="max-w-7xl mx-auto px-6 -mt-6 relative z-10">
        <div class="bg-white shadow-sm border border-gray-100 rounded-2xl p-6">
            <form method="GET" class="flex flex-wrap gap-4 items-end">
                <div class="flex-1 min-w-[280px]">
                    <label class="block text-xs font-medium text-gray-500 mb-1">RECHERCHER</label>
                    <input 
                        type="text" 
                        name="q" 
                        placeholder="Nom de recette, ingrédient..." 
                        value="<?= htmlspecialchars($mot_cle) ?>"
                        class="w-full px-5 py-3 border border-gray-200 rounded-xl focus:outline-none focus:border-[#D4AF37] transition">
                </div>

                <div>
                    <label class="block text-xs font-medium text-gray-500 mb-1">CATÉGORIE</label>
                    <select name="cat" class="px-5 py-3 border border-gray-200 rounded-xl focus:outline-none focus:border-[#D4AF37] transition">
                        <option value="">Toutes les catégories</option>
                        <option value="Entree" <?= ($_GET['cat'] ?? '') === 'Entree' ? 'selected' : '' ?>>Entrée</option>
                        <option value="Plat" <?= ($_GET['cat'] ?? '') === 'Plat' ? 'selected' : '' ?>>Plat</option>
                        <option value="Dessert" <?= ($_GET['cat'] ?? '') === 'Dessert' ? 'selected' : '' ?>>Dessert</option>
                    </select>
                </div>

                <div>
                    <label class="block text-xs font-medium text-gray-500 mb-1">TRIER PAR</label>
                    <select name="sort" class="px-5 py-3 border border-gray-200 rounded-xl focus:outline-none focus:border-[#D4AF37] transition">
                        <option value="">Pertinence</option>
                        <option value="temps_asc" <?= ($_GET['sort'] ?? '') === 'temps_asc' ? 'selected' : '' ?>>Temps (plus court)</option>
                        <option value="temps_desc" <?= ($_GET['sort'] ?? '') === 'temps_desc' ? 'selected' : '' ?>>Temps (plus long)</option>
                        <option value="recent" <?= ($_GET['sort'] ?? '') === 'recent' ? 'selected' : '' ?>>Plus récent</option>
                    </select>
                </div>

                <div class="flex gap-3">
                    <button type="submit" 
                            class="bg-[#D4AF37] hover:bg-[#C19A2F] text-white px-8 py-3 rounded-xl font-medium transition">
                        Rechercher
                    </button>
                    <a href="index.php" 
                       class="border border-gray-300 hover:bg-gray-50 px-6 py-3 rounded-xl text-sm font-medium transition">
                        Effacer
                    </a>
                </div>
            </form>
        </div>
    </div>

    <!-- Catalogue -->
    <div class="max-w-7xl mx-auto px-6 py-16">
        <div class="flex justify-between items-baseline mb-10">
            <h3 class="heading-font text-3xl font-light">Notre Collection</h3>
            <p class="text-sm text-gray-500"><?= count($liste_recettes) ?> recettes trouvées</p>
        </div>

        <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-8">
            <?php foreach ($liste_recettes as $recette): ?>
                <div class="card bg-white rounded-2xl overflow-hidden shadow-sm border border-gray-100">
                    <div class="relative h-64">
                        <img src="<?= htmlspecialchars($recette['image']) ?>" 
                             alt="<?= htmlspecialchars($recette['name']) ?>"
                             class="w-full h-full object-cover">
                        <div class="absolute top-4 right-4 bg-white/90 backdrop-blur-sm text-xs font-medium px-3 py-1 rounded-full">
                            <?= htmlspecialchars($recette['category']) ?>
                        </div>
                    </div>
                    <div class="p-6">
                        <h4 class="font-semibold text-lg leading-tight mb-2"><?= htmlspecialchars($recette['name']) ?></h4>
                        <div class="flex items-center justify-between text-sm">
                            <span class="text-gray-500">⏱️ <?= $recette['prep_time'] ?> min</span>
                            <span class="text-[#D4AF37] font-medium">Voir la recette →</span>
                        </div>
                    </div>
                </div>
            <?php endforeach; ?>
        </div>

        <?php if (empty($liste_recettes)): ?>
            <div class="text-center py-20">
                <p class="text-2xl text-gray-400">Aucune recette trouvée</p>
                <p class="text-gray-500 mt-2">Essayez de modifier vos filtres</p>
            </div>
        <?php endif; ?>
    </div>

    <!-- Footer -->
    <footer class="bg-gray-900 text-gray-400 py-12">
        <div class="max-w-7xl mx-auto px-6 text-center text-sm">
            <p>GoldenFork • Créé avec passion pour des expériences culinaires inoubliables</p>
        </div>
    </footer>

</body>
</html>